# 硬依赖(serve_xhs/gen_xhs 必须):
pyyaml
openai          # LLM 生成(Qwen 走 DashScope OpenAI 兼容端点)

# 可选——本地装了自动升级,不装走降级路径(Railway 部署不需要,体积大):
# sentence-transformers   # 语义相关度(run.py 用;生成器用不到)
# anthropic               # provider=anthropic 时
