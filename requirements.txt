# 硬依赖(必须):
pyyaml

# 可选——装了自动升级,不装走降级路径:
#   语义相关度(否则用关键词):
# sentence-transformers
#   LLM 成分分析(否则出占位)—— 按 config.yaml 的 provider 三选一:
# openai          # provider=qwen/openai(Qwen 走 DashScope 兼容端点)
# anthropic       # provider=anthropic
